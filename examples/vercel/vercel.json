{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "rewrites": [
    { "source": "/doc/:slug", "destination": "/api/doc?slug=:slug" },
    { "source": "/present/:slug", "destination": "/api/doc?present=:slug" }
  ]
}
